# /etc/nginx/conf.d/travel-rec-upgrade.conf
# WebSocket connection upgrade map for Streamlit.
# Define this once in the main nginx http context.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
